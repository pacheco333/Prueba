import { Routes } from '@angular/router';
import { LoginUsuarios } from './components/login-usuarios/login-usuarios.component';

export const LOGIN_ROUTES: Routes = [
  {
    path: '',
    component: LoginUsuarios
  }
];